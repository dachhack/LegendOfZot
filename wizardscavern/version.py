"""
Auto-generated version info. BUILD_NUMBER is incremented by pre-commit hook.
CHANGELOG is populated from recent git log entries.
"""

VERSION = "0.1.2"
BUILD_NUMBER = 324
# NOTE: Keep this list short (~8 entries). Remove old ones as new ones land.
CHANGELOG = [
    "M-cage flatline fixes (build 324). Build-323 5000T grid forensics traced 3 of the worst wasted-move runs to distinct shapes: s999_human + s512_human starved on F2 cycling U <-> reserved-'.' tile because U's only BFS-reachable neighbors were features (M auto-blocks transit, T auto-opens tomb_mode bouncing back); s1100_dwarf warp-jumped to F3 then died in 686T of retreat-mode deadlock because no U was reachable for ascent. Four coordinated fixes. (1) dungeon.py populate_rooms: reserve ONE plain '.' neighbor for U on subsequent floors so it always has a non-feature escape tile (kills the U <-> T 2-cycle baseline). (2) playtest_harness.py stairs_up_mode policy: M-cage escape ascent when tile_coverage.reachable<10 AND walkable>3*reachable AND turns_on_floor>100 (was previously blocked by retreat-only ascent). (3) PlaytestSession.m_caged_floors: tracks floors flagged via observe-time detection (visited<=5 + walkable>5*visited + 75T elapsed). Game_loop avoid_set + stairs_down_mode handler refuse to re-descend into a known M-caged floor, killing the F1<->F2 bounce that built up after s999 escaped the in-mode cycle. (4) Retreat give-up threshold 800T -> 400T so s1100-class warp-then-stuck cases clear retreat in time for the food clock. Smoke: s999 F2 stuck -> F1 starvation (T972->T1175); s512 F2 stuck -> F4 combat death (T873->T815); s1100 F3 stuck -> F4 starvation (T893->T1127). Tile-cycle pattern dead for all three.",
    "Revert U-camp escape (build 323). Build-322's stairs_up_cancels_this_floor counter + force-ascent branch verified DEAD CODE: across 50 runs, cancels_max==3 reached in 31 runs but forced_ascents==0 in every single run -- the existing retreat-target branch always satisfied first. Tile-cycle bug improved 15/50 -> 9/50 (likely measurement noise from obs-dict key drift), but new pathology emerged: floor-bouncing. s512_human had 104 descents + 103 ascents across F1-F2; net survival regressed (11 -> 9 @ t1500, starvation 24 -> 27). The cancel-escape doesn't address the real failure mode (region-split floors where D is genuinely unreachable AND warp jumps trigger perpetual retreat). Reverted cleanly. The build-321 sealed-pocket gen fix + blacksmith afford-gate remain; both shipped real improvements. Next floor-bounce work will need a retreat-counter / bank-current-floor mechanism, not a U-camp-specific fix.",
    "Flatline forensics fixes (build 321). 50-seed forensic pass on build 320 surfaced two new flatline shapes (the build-305 HP=1 case is dead, 0/50). (1) GAME-SIDE: 4/50 runs flatlined because drunk-carve dungeon-gen produced isolated 2-tile pockets disconnected from the main floor; when U landed in one, the agent cycled the same 2 tiles until starvation (s233_human 563T, s42_human 180T, s314_dwarf 146T). dungeon.py:carve_grid now flood-fills from (start_r=1, start_c=1) after carving and walls off any unreachable floor tiles. The existing path check only validated start->end connectivity, not whole-component reachability. Vaults are unaffected because they're carved AFTER this flood-fill (line 494 generate_vault_on_floor, which digs into walls post-population). (2) HARNESS-SIDE: s100_elf wedged 562T spamming '2' at the F5 blacksmith because smart policy checked 'is gear worn?' but not 'can I pay?'; process_blacksmith_action rejects with 'Not enough gold' and keeps prompt_cntl in blacksmith_mode, agent re-issues '2' forever. Added item_value to the equipped-slot obs dict + an affordability gate in the blacksmith_mode branch so the policy walks off when broke.",
    "Warp-aware fog descent (build 320). Build-319 verified the [V, SAFE, D] fog-aware override fired 132 times across 27/30 runs but only 22% led to descent within 100T (18% on F1) -- frontier-walker fallback works but is slow on region-split floors where D is genuinely behind walls. Worst case dwarf_s101 still sat 1048T on F1. The existing trapped_no_d branch already wires warps when turns_since_new >= 100, but the dawdle case slowly reveals one new tile every 50-90 turns (just under that threshold) so warp targeting never opens. Build 320 adds ('W',) ahead of ('D',) in the fog-aware override tiers, so the agent takes the random-teleport gamble when food is about to run out -- better than starving on F1 with a known W tile two rooms over. Wayfinder's W handler at line 3052 already exists; just needed the tier reference.",
    "Upgrade scrolls + permanent elixirs reachability pass (build 315). Build-314 playtest (24 runs, 8 each human/elf/dwarf) found 11/24 runs ended with ZERO upgrade scrolls and 0/24 ever saw a permanent stat elixir -- the F15/25/35 level gates put elixirs past the median death floor of F3. Six coordinated fixes: (1) chest scroll curve smoothed `min(0.30, 0.05 + z*0.025)`, removing the F4->F5 cliff (F1: 5% -> 7.5%, F4: 5% -> 15%). (2) chest scroll tiers extended past Divine to Celestial(F30+) / Cosmic(F35+) / Eternal(F40+) -- handler already supported them but only magic-shop hardcode could drop them. (3) Permanent elixir level gates 15/25/35 -> 8/16/25 so basic tier hits F6+ chest pool. Values unchanged; only reachability shifts. (4) Magic shop chance 0.15->0.20, min floor 20->12. (5) SCROLL_TEMPLATES gained Celestial/Cosmic/Eternal entries (referenced in items.py:2078-2087 handler but missing from random-pool source). (6) Smart-policy gaps: BUFF_POTION_TYPES gained dexterity/intelligence/experience/true_sight/invisibility (previously hoarded unused); new PERMANENT_POTION_TYPES set drives a no-HP-gate `i`-open from game_loop and an unconditional drink in the inventory branch so permanent elixirs are never carried to the grave.",
    "XP/kill % capped at 100% in summary_row (build 314). Build-313 grid still had 14/60 runs over 100% xp_pct because dynamic spawns (tomb-raid spirits, same-coord respawns across revisits, chest-gas mobs) credit the kill counter without updating the static M-tile pool. The build-309 -> 313 progression has chased every dynamic-spawn path I can find; the residual is real bonus XP that's hard to predict. Per playtest_report.py:786 lift the denominator to `max(static_pool, earned)` so the headline reads '% of MINIMUM available pool' and ratios are bounded at 100%. Per-floor rows in the Exploration table still show raw values for diagnosis. The verdict (high-xp_pct correlates with starvation deaths -- agent over-forages) remains robust whether the headline caps or not.",
    "XP pool accumulates on floor revisits (build 312). Build-311 grid still had 14/60 runs over 100% xp_pct after the spawn-level-offset fix; the playtester traced it to floor revisits respawning M tiles (Durin Forgewright s=314 dwarf had 6 floor changes and 188 kills against a pool snapshotted once at 83 M tiles -- kill_pct 226%). Fixed _snapshot_floor_totals to re-tally M + xp_pool on every arrival, accumulating the new spawns into the existing counts. Static-tile counters (C/V/T/N/B/F/G/L/O/A/P/Q/K/X) stay one-shot since those don't respawn. Re-snapshot trigger gated on `_last_snapshot_floor != pc.z` so it fires once per arrival, not every observe(). Earlier build-311 verdict (high xp_pct correlates with starvation, not survival) should now reproduce with truthful denominators.",
    "Carnyx of Doom XP bonus removed (build 311). User: 'kill the exp bonus for the carnyx. Not needed on that item.' Per blow the item already killed every monster in 8 adjacent squares (including diagonals) -- an enormous tactical benefit on its own; the additional +50 XP per kill was double-paying. game_systems.py:851-854 stripped: the kill log + 'No monsters in range' branch remain unchanged, only the gain_experience + bonus log line are removed. Side-effect: the xp_pool metric is now closer to the truth for runs that use the Carnyx, since carnyx kills no longer add untracked XP.",
    "XP pool formula fix (build 310). User: 'Looks like the formula needs help. a lot of % over 100%'. Build-309 grid had 11 runs with xp_pct > 100% because the per-M-tile estimate was a flat `(z+1)*8 + z*z*2` and ignored spawn-level offsets baked into game_systems.py spawn logic. Now reads each M tile's properties at snapshot time: tomb_elite -> floor+1 level (game_systems.py:2613), undead_guardian (non-elite) -> floor level, has_zots_guardian / is_boss_arena -> vault-defender XP (level+1)*100 (game_systems.py:727, 4x baseline). Spot check s=314 dwarf F5: 95/98 kills, xp_pct 97% (was 274% under the flat formula). Residual >100% cases would now come from non-tile XP (Carnyx kills, Experience Boost potions, library reads if any).",
    "Exploration efficiency metric (build 309). User: 'evaluate exp + treasure left on each level to see if better exploring would help with survival.' PlaytestSession.floor_totals[z] snapshots the floor's M / C / G / L / O / A / P / V / T / N / Q / K / B / F / X tile counts plus an expected xp_pool ((z+1)*8 + z*z*2 per M tile) on first arrival. playtest_report captures the snapshot per floor + the new xp_by_floor counter that sums kill-XP from log lines. summary_row exposes monsters_total / monsters_killed / kill_pct / xp_pool / xp_earned / xp_pct / chests_total / boons_total. Per-run page gains an Exploration section showing per-floor kills/M, XP/pool, chests, boons, vendor, tomb counts with overall totals. Index table gains an XP % column with the raw ratio. Smoke check on s=256 dwarf F6: 97/144 kills (67%) and 3218/4668 XP (69%) — clear signal that the agent is leaving real value on the floor.",
    "Cooking-kit polish (build 308). User: 'put the kit back on floor 3 vendor and have the player cook all their meat once they have the kit. Eating cooked meat is much better for survival.' Two changes: (1) Cooking Kit returns to F3+ vendor stock (was briefly F4+ in build 307; the original game tier was F3+ all along -- items.py:2499 comment 'Cooking Kit available from floor 3+'). Iron Rations stay at F1+. (2) Smart-policy cook-meat trigger drops the `hunger > 50` gate -- now fires whenever has_cooking_kit AND has_raw_meat exists. CookingKit.use() cooks ALL raw meat in one action, so this auto-converts the entire raw-meat backlog the moment the agent picks up the kit. urgent_meat (about-to-rot) still fires first because it's checked higher in the priority cascade. Earlier flatlines stay dead, s=13 elf still hits F9.",
]
