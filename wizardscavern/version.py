"""
Auto-generated version info. BUILD_NUMBER is incremented by pre-commit hook.
CHANGELOG is populated from recent git log entries.
"""

VERSION = "0.1.2"
BUILD_NUMBER = 328
# NOTE: Keep this list short (~8 entries). Remove old ones as new ones land.
CHANGELOG = [
    "Oscillation abort v2 -- unique-floor gate (build 328). Build-327 v1 aborted whenever 5+ floor changes accumulated in a 300T window. That cut off real progressors: 00023_elf in the B327 grid reached F7 with 7+ floor changes in normal descent and got abort-frozen at T1186 with legit_alive=True. Honest? No -- the run hadn't actually survived to T5000, the metric was inflated. Refined trigger at playtest_harness.py:1983: abort ONLY when `len(unique_floors) <= 2` in the window. F1<->F2 ping-pong: 2 unique -> abort. F1->F2->F3->F4->F5->F6: 6 unique -> let it run. F2<->F3 after F1 prunes out of window: unique drops to 2 -> abort. Smoke on 6 key seeds: 00023_elf now plays out to a natural dragon-death on F7 at T1290 (126 kills, 48.9% wasted -- just under legit threshold); the 4 bouncer seeds (13_dwarf, 89_elf, 512_human, 9999_elf) still abort correctly at fc=7, 2 unique floors; 00461_dwarf still starves on F3 (M-cage variant). The B328 legit_alive number will likely be lower than B327's 10/120 (those were partly abort-frozen artifacts) but more truthful -- the metric now requires runs to actually survive their full course, not just be frozen mid-bounce.",
    "Stair-tile softlock fix + oscillation abort (build 327). Build-326 autopsy on the 5 worst-loop runs in the B324 120-grid found 5/5 dying in a 'drown on the shore' pattern -- agent reaches a U or D tile, the handler's step-off-stair logic (intended to prevent ping-pong) overrules starvation pressure, and the food clock runs out while the agent shuffles. Worst cases: 00089_elf F2 spent 43 final moves on a single D-tile (step off / walk back / drink), identical for 00512_human; 09999_elf bounced F1<->F2 21 floor changes ending starved on F2 U. Two coordinated fixes. (1) STARVATION ESCAPE VALVE at the top of both stair handlers (playtest_harness.py:3864 stairs_down_mode, :4002 stairs_up_mode): if `starving` (hunger<=30 + no food in bag), immediately return 'd'/'u' to commit the transit. Bypasses M-caged, retreat, M-cage-up, and step-off branches. Rationale: descending refreshes fog + M drops = potential meat + new vendor; ascending returns to known-cleared territory. (2) OSCILLATION ABORT: PlaytestSession tracks `_floor_changes_recent` deque (300T window). When >=5 floor changes accumulate in that window, run aborts via is_done(). Prevents the valve from converting starvation deaths into 5000T loop-grinding (00013/00089/00512 went from dead-at-T~1000 to alive-at-T5000 with 90%+ wasted after valve-only; with abort, ends ~T1000 same as before but as 'alive but legit_alive=False'). Harness throughput preserved. Smoke: 4/5 autopsy seeds now correctly classified as loop class (`legit_alive=False`); 09999_elf aborts at T359 (was T1000+ starvation). 00461_dwarf still starves on F3 (different pathology -- M-cage blocks ascent, no D nearby for valve).",
    "legit_alive metric (build 326). User flagged that the 'alive @ T_cap' survival metric was contaminated -- of 10 nominally-alive runs in the deployed gh-pages set, only 1 (Bombur dwarf F5, 39% wasted, 87 moves, 23 kills, full HP) was genuinely progressing; the others were tile-cycling at >=50% wasted (worst was Halbarad human F2, 95.9% wasted, HP 1/40, 1541 moves). Added `legit_alive` bool + `legit_reason` to summary_row at playtest_report.py:744: `alive AND wasted_pct<50 AND moves_total>30 AND max_floor>=3`. Re-scored existing reports: B324 120-grid drops from 1/120 nominal to 0/120 legit; deployed 30-set drops from 10/30 nominal to 2/30 legit (Bombur + Imrahil F4 human). Honest baseline now established. Investigation surfaced systemic pathology: 82/120 (68%) of B324 runs end at >=60% wasted moves, deaths cluster on F2-F4 (61%), starvation 57% is the symptom of loop-induced food-clock exhaustion.",
    "Vendor restock arity fix (build 325). Build-324 120-seed grid surfaced a TypeError in 1/120 runs (s=167 dwarf): `generate_vendor_inventory() missing 1 required positional argument: 'room'` at items.py:2391. The function signature at items.py:2403 is `(floor_level, room)` and uses `room.properties.get('restocked', 0)` to scale a restock-penalty markup, but the call site in `restock_vendor_at` was dropping the kwarg. Fixed by passing `vendor_room` (already on hand from the room-type check two lines up). Non-fatal in the harness but a real game-side crash for the rare 'Scroll of Commerce + multi-vendor floor' path. Smoke: s=167 dwarf now reaches F5 (T1345 combat death) without traceback.",
    "M-cage flatline fixes (build 324). Build-323 5000T grid forensics traced 3 of the worst wasted-move runs to distinct shapes: s999_human + s512_human starved on F2 cycling U <-> reserved-'.' tile because U's only BFS-reachable neighbors were features (M auto-blocks transit, T auto-opens tomb_mode bouncing back); s1100_dwarf warp-jumped to F3 then died in 686T of retreat-mode deadlock because no U was reachable for ascent. Four coordinated fixes. (1) dungeon.py populate_rooms: reserve ONE plain '.' neighbor for U on subsequent floors so it always has a non-feature escape tile (kills the U <-> T 2-cycle baseline). (2) playtest_harness.py stairs_up_mode policy: M-cage escape ascent when tile_coverage.reachable<10 AND walkable>3*reachable AND turns_on_floor>100 (was previously blocked by retreat-only ascent). (3) PlaytestSession.m_caged_floors: tracks floors flagged via observe-time detection (visited<=5 + walkable>5*visited + 75T elapsed). Game_loop avoid_set + stairs_down_mode handler refuse to re-descend into a known M-caged floor. (4) Retreat give-up threshold 800T -> 400T so s1100-class warp-then-stuck cases clear retreat in time for the food clock. 120-seed grid confirms strict 2-cycle pattern killed (0/120), s1100 escapes F3->F6, aggregate survival statistically indistinguishable from B323 (Wilson CIs overlap).",
    "Revert U-camp escape (build 323). Build-322's stairs_up_cancels_this_floor counter + force-ascent branch verified DEAD CODE: across 50 runs, cancels_max==3 reached in 31 runs but forced_ascents==0 in every single run -- the existing retreat-target branch always satisfied first. Tile-cycle bug improved 15/50 -> 9/50 (likely measurement noise from obs-dict key drift), but new pathology emerged: floor-bouncing. s512_human had 104 descents + 103 ascents across F1-F2; net survival regressed (11 -> 9 @ t1500, starvation 24 -> 27). The cancel-escape doesn't address the real failure mode (region-split floors where D is genuinely unreachable AND warp jumps trigger perpetual retreat). Reverted cleanly. The build-321 sealed-pocket gen fix + blacksmith afford-gate remain; both shipped real improvements. Next floor-bounce work will need a retreat-counter / bank-current-floor mechanism, not a U-camp-specific fix.",
    "Flatline forensics fixes (build 321). 50-seed forensic pass on build 320 surfaced two new flatline shapes (the build-305 HP=1 case is dead, 0/50). (1) GAME-SIDE: 4/50 runs flatlined because drunk-carve dungeon-gen produced isolated 2-tile pockets disconnected from the main floor; when U landed in one, the agent cycled the same 2 tiles until starvation (s233_human 563T, s42_human 180T, s314_dwarf 146T). dungeon.py:carve_grid now flood-fills from (start_r=1, start_c=1) after carving and walls off any unreachable floor tiles. The existing path check only validated start->end connectivity, not whole-component reachability. Vaults are unaffected because they're carved AFTER this flood-fill (line 494 generate_vault_on_floor, which digs into walls post-population). (2) HARNESS-SIDE: s100_elf wedged 562T spamming '2' at the F5 blacksmith because smart policy checked 'is gear worn?' but not 'can I pay?'; process_blacksmith_action rejects with 'Not enough gold' and keeps prompt_cntl in blacksmith_mode, agent re-issues '2' forever. Added item_value to the equipped-slot obs dict + an affordability gate in the blacksmith_mode branch so the policy walks off when broke.",
    "Warp-aware fog descent (build 320). Build-319 verified the [V, SAFE, D] fog-aware override fired 132 times across 27/30 runs but only 22% led to descent within 100T (18% on F1) -- frontier-walker fallback works but is slow on region-split floors where D is genuinely behind walls. Worst case dwarf_s101 still sat 1048T on F1. The existing trapped_no_d branch already wires warps when turns_since_new >= 100, but the dawdle case slowly reveals one new tile every 50-90 turns (just under that threshold) so warp targeting never opens. Build 320 adds ('W',) ahead of ('D',) in the fog-aware override tiers, so the agent takes the random-teleport gamble when food is about to run out -- better than starving on F1 with a known W tile two rooms over. Wayfinder's W handler at line 3052 already exists; just needed the tier reference.",
]
