"""
Auto-generated version info. BUILD_NUMBER is incremented by pre-commit hook.
CHANGELOG is populated from recent git log entries.
"""

VERSION = "0.1.2"
BUILD_NUMBER = 325
# NOTE: Keep this list short (~8 entries). Remove old ones as new ones land.
CHANGELOG = [
    "Per-floor supply diagnostics in playtest harness (build 325, instrumentation only). Adds floor_diag tracking (chests_existing / chests_opened / vendor_existed / vendor_visited / vendor_initial+final+bought / rations_consumed / upgrade_scrolls_used / chest_bonus_queue_remaining), --diag-out CLI flag, and run-end JSON dump. 60-cell 3000T grid verdict: H1 dominates -- 52% vendor visit rate (range 70.6% F2 -> 13.3% F7), 41.5% chest open rate. When the agent DOES reach a vendor, rations bought 99.2%, upgrade scrolls 82.6%, heal pots 65%, fuel 40%. H2 (buy hesitation) is a minor leak. H3 (chests left unopened) is real: 15.7% of chest-floors had 0 chests opened, chest_bonus_queue median=6 of 9 supplement items left undelivered. Next levers: prioritize V tile on F4+ in wayfinder, lift descent gate to require >=50% chest-open rate.",
    "Scroll-upgrade menu now puts equipped gear first (build 325). Build-324 grid found equipped weapon mean upgrade_level=0.59 while best-in-bag=2.03 -- the 3-scrolls-per-floor budget was landing on unwielded weapons sitting in inventory. Root cause: process_upgrade_scroll_action at items.py:2115-2118 built the upgrade menu in insertion order, so once the agent picked up + auto-equipped a stronger chest weapon, the original Dagger stayed at slot 1 and ate every scroll. Smart-policy always picks '1' so all upgrades stacked on the unwielded starter. Fix: sort upgradable_items as [equipped_weapon, other_weapons..., equipped_armor, other_armors...]. Benefits real players too (wielded item shouldn't be buried). Smoke on s=42 dwarf reached F6 with equipped Battleaxe +3 (was +0 baseline). Should convert the per-floor scroll budget into actual combat capability and push the F4-F5 wall back.",
    "Per-floor guaranteed item supply (build 324). User: 'what if we seeded key drops on each level as the level gens. So 3-4 scrolls of upgrade across chests and the vendor. 5-10 rations, etc.' New floor_supply.py budgets 3 upgrade scrolls + 6 rations + 3 healing potions + 3 lantern fuel per floor, split as vendor supplement (1 + Rations(count=3) + 1 + 1 = 4 items) and chest-bonus queue (2 + 3 + 2 + 2 = 9 items). Attach hook in dungeon.py Tower.add_floor() runs at gen time; consume_vendor_supply called once per floor at vendor instantiation (regular dungeon vendor only); pop_chest_bonus runs BEFORE the chest's RNG roll so the bonus lands first. Scroll tier scales with floor. AUGMENTS existing RNG. Grid 324: alive 7 -> 11/60 (top of healthy band), starvation 33 -> 24, mean_floor 4.58 -> 4.73, F6+ 19 -> 22.",
    "CMA-ES quick-mode winner shipped as default PolicyConfig (build 323). 12 generations, pop=8, 15 seeds x 3 races x 3000T per eval, 32 min on 4 cores. Validation on 60 FRESH seeds x 3000T: alive=7 (vs build 320 manual best of 2), mean_max_floor=4.583 (vs 4.02), F4+=39/60, F6+=19/60 (~doubled). The first quick run had a horizon-mismatch failure (training 2000T, validation 3000T, winner alive=0) -- diagnosed as the 2000T-optimal profile (eat constantly, floor_stuck=125, fight-everything) being self-destructive at 3000T. v2 matched horizons. Tuned profile: attack_vs_flee_prob 0.92->0.98, spell_use_prob 0.90->0.996 (always cast), flee_level_gap_elite -1->-3 (flee elites even at -2 below player), floor_stuck_turns 300->178 paired with high_coverage_threshold 50->64, min_kills_floor_scale 2->1 (slower XP-vs-depth grind), eat_hunger 50->45, starving_threshold 30->44 (earlier inedible-flee). Saved as wizardscavern/data/policy_config_b323.json for posterity.",
    "CMA-ES tuning infrastructure for smart_policy (build 322). Pivot from one-knob-at-a-time grids to evolutionary tuning of all 23 policy thresholds at once. policy_config.py holds the PolicyConfig dataclass + FIELD_BOUNDS for every tunable (eat_hunger_threshold, heal_hp_critical_pct, floor_stuck_turns, flee_level_gap_general/undead/elite, spell_use_prob, attack_vs_flee_prob, etc.). smart_policy in playtest_harness.py was refactored to read all magic numbers from active_config() instead of hardcoded literals -- default config still reproduces build-321 behavior (smoke test passed). policy_tune.py wraps cma.CMAEvolutionStrategy in [0,1] unit space (bounded), parallel workers via mp.Pool, fitness = -mean_max_floor + survival-band penalty (target 3-8/60 alive at 3000T to preserve roguelike-hard feel). Quick mode (pop=8/gen=12/15-seed eval @ 3000T) ready to launch.",
    "XP/kill % capped at 100% in summary_row (build 314). Build-313 grid still had 14/60 runs over 100% xp_pct because dynamic spawns (tomb-raid spirits, same-coord respawns across revisits, chest-gas mobs) credit the kill counter without updating the static M-tile pool. The build-309 -> 313 progression has chased every dynamic-spawn path I can find; the residual is real bonus XP that's hard to predict. Per playtest_report.py:786 lift the denominator to `max(static_pool, earned)` so the headline reads '% of MINIMUM available pool' and ratios are bounded at 100%. Per-floor rows in the Exploration table still show raw values for diagnosis. The verdict (high-xp_pct correlates with starvation deaths -- agent over-forages) remains robust whether the headline caps or not.",
    "XP pool: encountered-monsters source + champion/platino markers (build 313). Build-312 grid still had 7/60 over 100% xp_pct because (a) champion / platino properties get set DURING interaction (not at floor entry), so the snapshot missed their +3 level offset, and (b) when monsters respawn on the same coordinate across floor revisits, encountered_monsters[(x,y,z)] only keeps the LATEST entry while kills_by_floor accumulates all kills. New approach: rebuild the pool from scratch each arrival using two sources of truth -- (1) gs.encountered_monsters[(x,y,z)] for tiles the agent has already interacted with (yields the real monster.level, handles champion/platino/spirit/elite upgrades), (2) static tile scan with property estimates for un-encountered M tiles. Also added is_champion (z+4 est) and is_platino (Lv 42) to the property-based estimate. Spot check: s=314 dwarf 97% / 97% (perfect), most runs land 80-95%. Residual same-coord respawn cases still possible but the median is now honest.",
    "XP pool accumulates on floor revisits (build 312). Build-311 grid still had 14/60 runs over 100% xp_pct after the spawn-level-offset fix; the playtester traced it to floor revisits respawning M tiles (Durin Forgewright s=314 dwarf had 6 floor changes and 188 kills against a pool snapshotted once at 83 M tiles -- kill_pct 226%). Fixed _snapshot_floor_totals to re-tally M + xp_pool on every arrival, accumulating the new spawns into the existing counts. Static-tile counters (C/V/T/N/B/F/G/L/O/A/P/Q/K/X) stay one-shot since those don't respawn. Re-snapshot trigger gated on `_last_snapshot_floor != pc.z` so it fires once per arrival, not every observe(). Earlier build-311 verdict (high xp_pct correlates with starvation, not survival) should now reproduce with truthful denominators.",
]
