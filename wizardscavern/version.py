"""
Auto-generated version info. BUILD_NUMBER is incremented by pre-commit hook.
CHANGELOG is populated from recent git log entries.
"""

VERSION = "0.1.2"
BUILD_NUMBER = 360
# NOTE: Keep this list short (~8 entries). Remove old ones as new ones land.
CHANGELOG = [
    "Shrink auto-unequips normal-size gear (build 360). User-reported bug: Bombur Axebreaker (s=10 dwarf) wound up with Adamantine Armor + Scorpion Tail Whip co-equipped -- normal-size armor and bug-size weapon at the same time. Root cause: the equip gate at game_systems.py:2263 only blocks NEW equips of the wrong size, not gear that was already equipped before the size change. _restore_size_on_bug_exit already drops bug gear when growing back, but the SHRINK direction had no symmetric unequip -- so a player descending into a bug floor in full Adamantine kit kept the armor through the shrink, then swapped the weapon slot to a bug weapon and ended up in a mixed state. Fix: _trigger_shrinking_spell now unequips any non-bug Weapon/Armor at the moment of shrinking, with flavor logs ('suddenly enormous in your tiny hands', 'slides off your shrunken frame'). Items stay in inventory so they auto-re-equip on grow-back.",
    "Resistance-potion crash fix (build 359). Build-358 7000T sweep surfaced 2/36 runs dying inside `TypeError: Character.add_status_effect() got an unexpected keyword argument 'resistance_element'`. Triggered when the agent drinks a Fireward / Frostward / Stormward / Shadowward / Lightward / Universal-resist elixir (any Potion with potion_type='resistance'). Call site items.py:1188 passes resistance_element=element, but both add_status_effect overloads (Character at characters.py:1450, Monster at 1727) had the old 5-arg signature. StatusEffect.__init__ at characters.py:582 already accepted resistance_element=None for years -- the wrapper just never plumbed it through. Both add_status_effect signatures now accept resistance_element=None and forward to StatusEffect. Smoke test: Potion('Fire', resistance_element='Fire').use(pc) now returns True and registers the Fire Resistance effect cleanly.",
    "F1-F2 tomb gating + pool damage scaling + lantern fuel buff + loop squashes (build 358). 7000-turn playtest exposed four issues: (1) L1 dwarf/human one-shot by F1-F2 tomb guardians (30 HP -> -1 in 4 turns), (2) elf one-shot by golden-pool mimic (-90 dmg vs 28 max HP), (3) F1 dark-pocket runs losing lantern fuel mid-floor, (4) two distinct loops: dwarf/11 F4<->F5 stair ping-pong driven by retreat-to-permanent-wedge-floor, and dwarf/9 F1 trapped-on-floor with D unreachable for 454T before starvation. Fixes: tomb (T) rooms now floor 3+ only (dungeon.py:actual_t_limits, parallels B/F/Q/K patterns). Pool damage outcomes (cursed/golden mimic/explosion/poison/slip) scale linearly with floor depth -- F1 mimic 20-40 vs F10 65-104 (was flat 50-100), so starter HP pools aren't one-shot but deep floors keep the bite. Starting shop lantern fuel 50 -> 80 + 2 Lantern Fuel canisters (40 fires) for 120-fire total cushion; harness mirrors. Loop fix 1: retreat_to_floor suppressed when destination is in permanent_wedge_floors, so warp-to-deep-floor agents bank current depth instead of climbing toward an impassable F2. Loop fix 2: warp-accept gate adds 5th firing path -- if D unreachable AND a W is visible AND turns_on_floor >= 200, accept; old gate needed reach_pct>=80% OR ts_new>=500 OR turns_on_floor>=600 which dwarf/9 never hit before starving.",
    "Playtest harness starting loadout aligned to real F1 shop (build 357). Prior harness handed the agent an inflated bundle: Lantern fuel=80 (real game stocks 50), 8 Rations (real=5), 4 Iron Rations + 2 Lantern Fuel canisters (NOT on the starting shop -- those are only available from later vendors), and a fixed 4 Minor Healing Potions with gold flat-set to 290. Real players walk out of the starting shop with one weapon, one Leather Armor, one Lantern (fuel=50), one Rations (count=5), one CookingKit, and 3-5 healing potions, with 500 gold minus the bundle cost. Harness now mirrors that exactly: rolls 3-5 potions off _stdlib_random (seed-stable), drops the inflated extras, and computes residual gold from the actual bundle cost so the early-floor food/fuel pressure matches what real players hit.",
    "Cantrips redesigned as utility-focused racial magic (build 356). Initial 355 design was four damage cantrips (Mote/Frost Bite/Pebble/Mind Touch) -- felt flat, just element variants. User pivoted to a mixed utility/combat pool: (1) Detect Monster -- scans 3x3 around caster, logs each monster's name + level + (col,row), and reveals the tile. spell_type='detect_monster', new handler in characters.cast_spell + combat._execute_charged_spell. (2) Light -- 5x5 fog-of-war reveal around the caster. spell_type='reveal_fog'. (3) Hold Monster -- reuses existing 'debuff_target' + 'time_stop' status_effect for 1 turn (combat.py:199 already skips the monster's attack when time_stop is on); enables flee or free-attack. (4) Mind Touch unchanged (1 MP Psionic damage). can_cast_spells() gate relaxed: previously max_mana>0 AND max_slots>0; now allows casters with at least one cantrip in memorized_spells even at 0 slots (fresh elf at INT 12). Spell-casting-mode now routes back to game_loop instead of combat_mode after a successful out-of-combat cast, so Detect/Light work as exploration tools. Harness default cantrip pair = Hold Monster + Mind Touch (combat-relevant). Picker UI shows utility-cantrip metadata as 'X MP - utility' (no 'power 0' on Detect/Light).",
    "Elf cantrips + lowered casting threshold (build 355). Build-354 playtester surfaced an elf-cohort collapse: 30/45 elves never reached F2 because they start at INT 12 / 0 mana and can't cast their memorised spells. Three-part fix: (1) Elf mana gate dropped INT 15 -> INT 11 with a softer slope (was (int-15)*4+10, now (int-11)*3+5) -- INT 12 yields 8 MP, INT 16 yields 20 MP, INT 25 yields 47 MP. (2) Four new cantrips, 1 MP each, slot-free. (3) New player_cantrips character-creation screen between portrait pick and starting shop -- elves pick exactly 2 of 4. Non-elves skip straight to the starting shop.",
    "V (vendor) added to floor-connectivity guarantee (build 354). User: 'V should always be reachable.' Build-353 validation showed ~10% of F1-F4 deaths were on runs that never visited a vendor because V was walled-off from the spawn pocket. dungeon._ensure_d_reachable already carved corridors to D and W when they were unreachable from the entry tile; extended the target tuple to include V. Same L-shape wall-carve through walls (NOT through M/W hazard rooms, which the agent's BFS still treats as transit blockers). Vendors are the only weapon / armor upgrade path for non-caster races, so a no-V spawn pocket is as bad as a no-D one.",
    "Gear-upgrade vendor pull on F1-F2 (build 353). Build-352 validation: 11/14 base-Dagger F1-F4 deaths were from runs that NEVER visited a vendor with 250-800g unspent. Cause: wants_vendor only triggered on (healing_count < 2 OR needs_repair) AND gold >= 50, but the 4-potion starter pack keeps healing_count >= 4 through F1-F2, so the wayfinder never actively pathed to V -- it just descended past. New trigger: weak_starter_weapon (atk_bonus <= 2 = Dagger) OR weak_starter_armor (def_bonus <= 3 = Leather), AND gold >= 25 (Short Sword price). The cheap-upgrade gold floor is far below the healing-need floor because early floors yield 25g easily after 1-2 kills. With the build-352 vendor-buy priority already in place, every triggered V visit should now produce an equipped Short Sword / Mace / Iron Sword.",
]
