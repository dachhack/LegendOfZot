"""
Auto-generated version info. BUILD_NUMBER is incremented by pre-commit hook.
CHANGELOG is populated from recent git log entries.
"""

VERSION = "0.1.2"
BUILD_NUMBER = 322
# NOTE: Keep this list short (~8 entries). Remove old ones as new ones land.
CHANGELOG = [
    "F1-F3 food bottleneck cracked: +120% alive runs, +13% avg depth (build 322). User: 'Let's see what is bottlenecking f1-3.' 160-seed audit revealed 109/160 (68%) of grid deaths were starvation, dominated by F1-F3 -- agents were not getting the food multipliers the design intended. Chased it down through five bugs: (1) vendor.py's dungeon-vendor inventory was dead-code-bypassing items.py:generate_vendor_inventory and never stocked CookingKit / Iron Rations / Salted Jerky -- 0/160 runs had any of those items. (2) Even items.py's floor-tier check was off-by-one (`floor_level >= 3` against character.z meant F4+, not the documented F3+). Fixed both: dungeon vendors now stock Iron Rations at F1+, Jerky at F2+, Cooking Kit at F3+; starting vendor stocks a kit too. (3) smart_policy's vendor branch never had a 'buy cooking kit' rule -- it was filling the STOCK dict for potions / fuel / food but the kit fell through. Added a one-time kit-purchase priority above rations (saves the run permanently for ~120g). (4) Added Cooking Kit to the playtest starter pack (the harness bypasses the starting shop UI). (5) Smart_policy's eat-when-hungry gate picked the first 'food' entry which sorted rations above meat, so the agent burned through rations while raw meat rotted in the bag. Added meat_slot/eat_slot preferring meat (perishable) over rations (vendor-restockable). Trace on s=666 elf (114 raw-meat samples, 34 cook actions, only 2 meats eaten before starving): the eat-priority flip alone should keep that bag from rotting. Grid result vs baseline: 110 -> 95 starvation deaths, alive 5 -> 11 (+120%), avg max floor 3.07 -> 3.47 (+13%), F8+ alive count 0 -> 8. The remaining starvations are mostly F1-F2 runs that die before finding monsters with meat drops -- a separate gate (early-floor monster engagement) for the next round.",
    "Warp avoidance pass: -46% warps across the grid (build 321). User: 'playtester avoiding warps. They shouldn't be hitting those rooms unless absolutely necessary to finish gathering loot and hitting exploration thresholds.' Build-320's transition classifier revealed 229/647 (35%) floor changes were warps -- 194 forced + 35 voluntary -- mostly hidden under 'other' before the timing fix. Two paths needed work: (1) trapped_no_d gate in game_loop + warp_mode was firing at 100T-since-new-tile alone; tightened to require reach_pct>=90 AND turns_since_new>=200, so the agent only targets W when it has truly swept its reachable region (was popping the valve on agents still uncovering ground via lantern reveals). (2) Step-off helpers were hazard-blind: flee_direction_mode would bolt from one M onto an adjacent W (28 first-warps in the grid), chest_mode's _walk_off_chest accepted any non-wall direction (13 first-warps), _step_away for oracle/war_room/taxidermist did the same. All three now do non-recent-non-hazard first, then drop the recent filter, then accept hazards only as the last resort. Grid result: 229 warps -> 123 (-46%), total floor transitions 647 -> 402 (agents progressing more linearly instead of warp-pinballing), warp share 35% -> 30%. 21/28 F1 deaths are now starvation rather than warp-induced softlocks on later floors. The remaining warp_forced bucket is mostly genuine map-fragment cases where the policy correctly chose to escape.",
    "Floor-transition classifier fixed: timing bug + multi-floor jump support (build 320). User screenshot of s=461 human Journey tab showed every transition labelled '(other)' including obvious F3->F5 / F5->F7 multi-floor jumps that should read as warps. Two bugs: (1) record_action was called AFTER sess.step(), so obs['mode'] was the POST-step mode -- a normal descent ('d' in stairs_down_mode) stored as ('stairs_up_mode', 'd') because step() had landed the agent on the new floor's U tile by the time we measured. Classifier checks mode_at=='stairs_down_mode' which never matched -> everything fell to 'other'. Fix: capture pre_mode = obs['mode'] BEFORE sess.step() and pass that to record_action. (2) Multi-floor jumps (Scroll of Descent rolls 1-3 floors, forced-warp lands +/-2) had no classifier branch. Added: inventory + any jump -> 'scroll_descent' / 'scroll_ascent' (purple in journey list), abs(jump)>=2 with no other tag -> 'warp_forced'. Journey-line annotation adds '(skipped N floors)' for any abs(jump)>=2 so the user can spot the multi-floor hop at a glance. 159-seed grid: 329 stairs_down + 194 warp_forced + 82 stairs_up + 35 warp_accept + 7 scroll_descent + 0 'other' (was 100% 'other' before). The 35% warp share of transitions was hidden the whole time.",
    "Loop hunt round 4: 2-tile wayfinder oscillation guard (build 319). Mid-run loop scanner from build-318 showed game_loop period-2 ('ns' / 'ew') as the dominant remaining wedge type -- 19 cases on the 159-seed grid, biggest s=23 dwarf F1 (9,14)<->(10,14) for 392 turns post-shrine while surrounded by 3 monsters the policy was avoiding. Root cause: the 4-entry recent_positions buffer is too short to identify the bounce; both tiles end up 'recent' so swap-if-backtrack finds no alternative and the wayfinder returns the same BFS first_step every frame. Fix: added a 16-entry position_history buffer reset on floor change, plus an _oscillation_state detector that trips when the last 12 frames cover exactly 2 unique tiles with >=5 visits each. Exposed in obs.oscillation; smart_policy's game_loop branch runs an anti-oscillation gate before the wayfinder kicks in. The gate prefers a non-pair walkable direction, falls through to M (combat) when the agent is surrounded by hazards, then W (warp), then yields back to the wayfinder. Gated on hp_pct >= 0.50 and not immobilised so a 30% HP agent doesn't get shoved into a fight. Tried 3-tile cycle detection too but it regressed (false-positives on legitimate short-corridor exploration via fog-frontier BFS); reverted to 2-tile only. Also filter combat_mode / spell_casting_mode / flee_direction_mode out of the report's mid-run loop scan -- long fights are healthy progress, not wedges. 159-seed grid: 38 runs with mid-run loops (was 54), 53 episodes (was 79), 5553 turns wasted (was 9830, 43% reduction). 9 alive (was 7).",
    "Loop hunt round 3: mid-run loop episode scanner + stair-island descent valve (build 318). User: 'Are there any runs with loops within runs? (Not just ended on a loop)' Built a whole-run post-hoc scan: RunReport._scan_loop_episodes slides a 20-action window across the entire action history, marks every frame where the period-1..4 detector trips with 12+ matches, merges consecutive marked frames into episodes >= 50 turns, picks the dominant cycle (period-length action substring sampled from the episode mid-point so the label reflects the steady-state, not the boundary transition). Each episode reports (turn range, length, mode, cycle, period). summary_row exposes loop_episode_count + loop_turns_total; per-run page gets a 'Mid-run Loop Episodes' table; index gets a Loops column tinted amber when >= 200 turns wasted. 159-seed grid: 54 runs with loops, 79 episodes, 9830 turns wasted total. Top signatures: game_loop ns/ew period-2 (wayfinder oscillation), stairs_up_mode ew (passing-through wedges), combat_mode a/fwe. Stair-island fix: when stairs_down_mode's grind-first step-off finds the only non-wall non-hazard neighbour is the OTHER stair (U), press 'd' to descend past the tiny stair-island floor instead of oscillating forever. Caught s=1234 human F3 (1,1)U <-> (2,1)D 99-turn ping-pong and 4 similar runs. Earlier attempt added the symmetric valve to stairs_up_mode but the two valves cancelled out (descend -> land on U island -> ascend -> land on D island -> ...), surfaced as 87/159 stuck in the test grid; reverted to the one-sided descend-only valve.",
    "Loop hunt round 2: upgrade-scroll targeting + flatline detection + taxidermist softlock (build 317). (1) Scroll-of-Upgrade slot mismatch: process_upgrade_scroll_action and process_identify_scroll_action iterated `inventory.items` in raw insertion order while the rest of the game uses get_sorted_inventory. Smart-policy aimed for the equipped weapon's sorted slot, so the upgrade landed on whichever junk pickup entered the bag first. Playtester scanned s=1 dwarf consuming 9 scrolls with the equipped Halberd still at +0 upg. Both scroll handlers now iterate the sorted view. (2) Equipped-weapon targeting in smart_policy.upgrade_scroll_mode: '1' fallback replaced with explicit lookup of the equipped weapon's position in the gear-only sorted view (weapon preferred over armor when both equipped). Hit-rate jumped from ~25% to 57% across the 159-seed grid. (3) Active-scroll-tier awareness: added active_scroll_item to obs + scroll_name on inventory entries so the inventory-mode reader skips upgrade scrolls when the equipped weapon is already at the scroll's tier cap (Basic +3, Greater +6, ... Eternal +35) AND skips when no weapon is equipped. Caught s=941 elf F9 at 504 idle turns spinning u<scroll>->1->c after the Halberd broke. The wedge Hail Mary path got the same gate. (4) Tighter flatline classifier: window narrowed from max(100, turns//4) to max(80, turns//5); HP-stability eased from max-min<=1 to <=3; new-tile-reached added to the progress signal set so an exploring fog-walker doesn't get flagged stuck. (5) Taxidermist softlock: process_taxidermist_action only accepts digit / 's' / 'i' / 'x' but the action hint claimed n/s/e/w worked too; policy returned _step_away() (a direction) when no trophies in bag, which the handler ignored, taxidermist_mode persisted forever. Caught s=941 elf F9 at 500 idle turns mashing 'w'. Hint corrected, policy returns 'x'. 159-seed × 3-race wide hunt: 0 stuck after fixes.",
    "Loop hunt round 1: three softlock fixes surfaced by status=stuck (build 316). (1) Variable-shadowing in playtest_report.to_html: the items-table aggregation loop reused the name 'status' for per-item bag-state, clobbering the outer alive/dead/stuck classifier value before the death_block branch and dropping the stuck-banner from the per-run page. Renamed to item_status (line 1161). (2) Blacksmith afford-gate: smart_policy returned '1'/'2' on worn gear without checking gold; the handler logs 'Not enough gold' but stays in blacksmith_mode, infinite loop. Added _repair_cost_est helper (80% of get_repair_cost) to the equipped slot dict and gated the repair returns on gold >= cost. Caught on s=1234 dwarf F5 (850+ turns mashing '2' with 4g). (3) Shrunk-stairs gate: Zot's shrinking spell on bug-levels blocks descent + ascent ('drop is lethal', 'stairs impossibly tall'); the handler stays in stairs_down_mode / stairs_up_mode and the policy mashed 'd' or 'u' forever. Added is_shrunk early-return that walks off the stair tile, plus a tier filter in game_loop wayfinder that strips D + U from priority tiers while shrunk so the agent doesn't immediately route back. Caught on s=461 elf F8 (176+ idle turns at HP 78/78). 159-seed × 3-race wide-hunt now shows 0 stuck (was 2 prior).",
    "Three-state playtest outcome on the report page (build 315). 'alive vs dead' missed a third case the playtester kept hitting: agents finishing the turn budget alive but having clearly stopped progressing. RunReport._classify_status now distinguishes 'stuck' (alive + no progress event in the last max(100, turns//4) turns with HP-stable, OR a tight 1-4 period action cycle with at least 30 idle turns) from genuine 'alive' (still gaining XP, descending, or buying). status + status_reason ride the JSON sidecar so both the per-run page and the index render the new amber tag. Healthy n/e/s/w explore loops with XP gains stay 'alive' because the loop branch requires idle_turns >= 30. Synthetic ping-pong wedge → stuck (loop period=2); long flatline → stuck (flatline ...); normal explore → alive. Death always wins.",
]
