"""
Auto-generated version info. BUILD_NUMBER is incremented by pre-commit hook.
CHANGELOG is populated from recent git log entries.
"""

VERSION = "0.1.2"
BUILD_NUMBER = 441
# NOTE: Keep this list short (~8 entries). Remove old ones as new ones land.
CHANGELOG = [
    "b441: re-anchored the combat loom so the towering monster stands right above you. The loomed sprite was bottom-anchored inside a fixed ~78px slot (position:relative), so its feet sat mid-panel and the creature floated up in the middle of its box. Now the slot is just an in-flow width spacer (no position), so the sprite's absolute bottom:0/left:0 wrapper resolves to #monster_panel itself -- planting the creature's feet at the bottom-left of its box, immediately above the player box, with the body towering UP and spilling out of the box top (still painted over the stats bar by the existing overlay). Applies to the combat, spell, flee, and victory frames.",
    "b440: polished the elite/boss combat loom after the b439 fix. (1) The VICTORY frame no longer drops the loom -- a felled elite kept rendering as a 96px in-flow sprite that grew the monster box and clipped the player box below it; it now reuses the fight's loom styling (slot-capped panel height + the over-the-HUD overlay), so the dead foe still towers on the LEFT over the stats bar instead of expanding the panel. (2) The defeat grayscale fade now also greys the loom overlay -- when looming, the visible sprite IS the overlay (the in-flow canvas is hidden), so without this the dead-monster fade did nothing. (3) Loomed sprites now anchor to the left edge of their slot.",
    "b439: fixed the loom (elite/legendary/boss combat sprites) rendering the monster TWICE -- a copy glued to the panel text plus a second copy drooping down into the player panel below it. The loom paints a fixed over-the-HUD overlay positioned at the in-flow canvas's rect, but it snapshotted that rect at img.onload while the panel's combatIn entrance (a center-scale animation that replays on every combat re-render) was still mid-flight, so the overlay locked to a stale, lower position and never realigned. Now the in-flow canvas is hidden (kept only as an invisible layout anchor) so the sprite is painted exactly once, and the single overlay TRACKS the anchor's rect across the entrance via a short requestAnimationFrame loop instead of a one-shot snapshot. Stale overlays are also deduped before each mount.",
    "b438: the 41 marquee 'strong monsters' (Cinder Serpent, Sphinx, the deep liches/fiends/titans/elementals, etc.) now LOOM in combat regardless of your level. The threat-tier system sized a monster purely by how far it out-levels you, so an over-levelled hero saw a Cinder Serpent render as a tiny 50px 'trivial' icon despite its bespoke art. Added a new 'elite' threat tier (118px sprite, over-the-HUD loom overlay, orange ELITE badge, tier-3 entrance flourish), and _classify_monster_threat now floors any of the 41 strong-monster keys to it. Flagged champion/legendary/boss spawns are still classified first and keep their higher tier, and ordinary fodder still scales relatively with no loom.",
    "b437: finished the flavor/sprite consistency pass -- swapped the two remaining mismatched strong-monster sprites to match their lore. ANCIENT (the Treant) now renders a leafy, bark-bodied tree-man (atlas s12_14) instead of a crowned lich, and LORD (the Water Elemental Lord) now renders a flowing blue water-being (s12_18) instead of a druid. Both reuse their existing pids (MN0590, MN2912) with the pool image + sprite_picks overlay overwritten. All 52 strong monsters now match their flavor text.",
    "b436: flavor/sprite consistency pass over the 52 strong monsters -- after the b434/b435 art swap, checked every monster's flavor_text against its new sprite and caught three clashes. Fixed the first: Bonepicker Reaver was written as a skeleton ('clatters apart into a heap of yellowed bone'), but its hand-picked sprite is a fleshy six-armed blade reaver, so its flavor + victory text were rewritten to match (a many-armed marauder that strips the dead to the bone). The other two (ANCIENT/Treant showing a crowned lich, LORD/Water Elemental showing a druid) are sprite-swaps still in progress.",
    "b435: Bonepicker Reaver joins the strong-monster art pass -- the 52nd key now renders a gaunt six-armed, blade-fisted reaver from the strong-monsters atlas (cell s09_03) in place of its old round-8 sprite. Same pipeline as b434: pid MN2535's pool image + its sprite_picks/monsters overlay overwritten with the 96x96 webp, and _MONSTERS_MAP collapsed to the single chosen variant.",
    "b434: fresh art pass for the top of the bestiary -- all 51 strong-monster keys (the 11 bosses/legendaries plus the 40 shard-guardian & deep-tier elites) now render hand-picked sprites from the new Gemini strong-monsters atlas instead of the old round-8 picks. Each key was matched to one sprite via a custom in-browser picker; the chosen cells are tight-framed to ~68% and encoded the pool's way (96x96 RGBA webp q80), patched into canonical_pool_full.pkl, and mirrored as sprite_picks/monsters overlays so a future promote_all_sprites rebuild keeps them. _MONSTERS_MAP was repointed key-by-key (reusing each key's existing primary pid, so no orphans) and collapsed to the single chosen variant. ZOT'S GUARDIAN -- previously special-cased with no sprite -- gets its first dedicated entry (new pid MN5000).",
]

