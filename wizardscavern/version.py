"""
Auto-generated version info. BUILD_NUMBER is incremented by pre-commit hook.
CHANGELOG is populated from recent git log entries.
"""

VERSION = "0.1.2"
BUILD_NUMBER = 453
# NOTE: Keep this list short (~8 entries). Remove old ones as new ones land.
CHANGELOG = [
    "b453: two combat-feel fixes. (1) Fleeing no longer takes three taps -- FLEE was registered as a 'dangerous command' so it armed a two-tap 'tap again to confirm' before even rolling, on top of the direction d-pad that follows a successful escape. But flee is a defensive escape, not an irreversible act like deleting a save or sacrificing at an altar, and picking a flee direction (with FIGHT to cancel) is already a natural confirm. Dropped 'f' from the dangerous-command list, so it's now FLEE -> roll -> pick a direction. (2) The monster-defeat animation timeline is now tunable from one spot: the hardcoded 2100ms grayscale, 2700ms DEFEATED-overlay, and 4500ms panel-auto-dismiss are now DEFEAT_GRAYSCALE_MS / DEFEAT_OVERLAY_MS / DEFEAT_DISMISS_MS in game_state.py, read by generate_monster_defeat_js and the dismiss timer. The log/toast reveal (DEFEAT_LOG_REVEAL_MS) now derives from DEFEAT_OVERLAY_MS + 600 so it always lands just after the overlay flashes, even if the timeline is retuned.",
    "b452: dropped-item loot toasts no longer spoil a kill either. The top-right '[icon] You found a ...! / +N gold' banners were pushed onto the queue the instant a monster died and popped up immediately -- announcing the loot (and therefore the win) while the dice were still tumbling and the DEFEATED overlay (2.7s) hadn't even flashed. Toasts now carry a per-toast reveal_delay: combat kills stamp 3.3s (set once in _clear_combat_visuals_on_kill, the single chokepoint all four kill paths pass through; reset after render like monster_defeated_anim), while chest/altar/room pickups stay instant at 0. Implemented purely in CSS timing -- the toast still renders on the kill frame but its animation-delay goes positive so the fade hasn't started, and the keyframe fill-mode flipped forwards->both so the held toast sits at opacity 0 instead of flashing visible. No extra re-render needed; the existing age-resync on every render keeps multi-toast stacks aligned.",
    "b451: combat log no longer spoils results, and no longer blanks the whole log to hide them. Each new log line now carries a per-line reveal delay: lines already on screen stay visible, and only the new combat-result lines fade in once their animation has resolved (a dice exchange at ~3.3s, an init-only roll at ~1s). The big fix is kills that set the defeat animation but roll NO dice -- spell kills and start-of-turn status deaths -- which previously dumped 'You defeated the X! / You harvested meat!' into the log INSTANTLY, while the grayscale fade (2.1s) and DEFEATED overlay (2.7s) were still playing. Those lines now hold until 3.3s, landing after the overlay flashes in, so you never read the kill before you see it. Replaces the old whole-log opacity:0 hide in both render paths (initial wrap_html + incremental updateGame), unified behind one updateLog() that schedules per-delay reveals with a render token to drop stale timeouts.",
    "b450: unified the four copies of the sprite <canvas>+image-loader <script> boilerplate behind one helper, sprites/pool.py:render_sprite_canvas(). The monster / room / player renderers in sprite_data.py and the inline item-icon renderer in sprites/identifiables.py all drew a pool webp into a pixelated 2D canvas with the same JS skeleton, differing only in DOM id, size, canvas CSS, an optional wrapper div (monster/player need it so loom/flourish overlays can escape the canvas), and the monster's onload effect hook. They now share one renderer. Proven faithful by a 3,720-sample render digest -- every monster across seeds/sizes/loom+flourish tiers, every room type, every race, plus 60 item icons -- byte-identical before and after (sha dea6497f). Playtest still matches baseline. Part five of the cleanup pass.",
    "b449: de-duplicated the vendor-shop list HTML. The buy/sell screen and the repair/identify screen each contained their own ~40-line copy of the 'Vendor Wares' + 'Your Inventory' panel builders -- the wares half was byte-for-byte identical, the inventory half differed only in the row tap-prefix. Both copies now call three shared module helpers (_render_vendor_wares_html, _render_player_inv_html(tap_prefix), and a _render_tappable_row primitive). Proven faithful: with the per-render DOM-id counter held fixed, the helpers emit output byte-identical to the old inline blocks across all five vendor_action states (buy/sell/repair/identify/none). 200-turn playtest still byte-identical to baseline. Part four of the cleanup pass.",
    "b448: deleted 12 confirmed-dead functions/methods (~555 lines, zero callers). The big two were a superseded inline-audio path in app.py: generate_inline_music_js (120 lines) and build_audio_player_html (165) -- both replaced by the persistent _musicEngine shell. Crucially their shared _MUSIC_SONGS_JSON song data is NOT dead (the live engine still embeds it at render time), so it was kept. Also removed handle_warp_room/get_equipment_display_name/get_evolution_tier_style (game_systems), vendor_identify_item/get_hunger_label/remove_giant_strength_on_expire (items), count_room_cells (dungeon), strip_ansi (app), and the Inventory.check_item/display_inventory methods (characters) -- the latter also carried a latent NameError (item_str instead of item_display) that ruff flagged as F821, now gone with the dead code. Each verified caller-free across game + tools before removal. Full suite green: every module + app.py import, F821 clean, and the 200-turn smart playtest log byte-identical to baseline across 5 seeds. Part three of the cleanup pass.",
    "b447: removed 162 unused imports across 13 files (F401). Care was needed because app.py does `from .items import *`, `from .game_systems import *`, etc., so a name that looks unused in a source module can actually be re-exported into app.py's namespace and referenced bare there. Verified safe with an AST pass (ignores strings/comments, which had faked several 'uses' -- e.g. the display text f'Floor {z+1}' is not a reference to the Floor class) confirming every removed/lost symbol is either never loaded as code in app.py or has its own local import. Full suite green: all modules import standalone, app.py imports under a toga stub, and the 200-turn smart playtest log is byte-identical to baseline. Part two of the cleanup pass.",
    "b446: mechanical dead-code sweep, no behaviour change. Removed 52 f-strings that had no placeholders (f\"...\" -> \"...\"), 59 unused local variables, and one unreachable block in combat.py (a 'Your Memorized Spells:' listing sat after an unconditional return inside the cmd=='init' branch, so it could never run). Net ~75 lines gone across 9 files. Verified by a 200-turn smart playtest whose full log is byte-identical to the pre-change baseline, plus clean runs on seeds 1/7/42. Import-star re-exports were deliberately left untouched. First of a multi-step cleanup pass.",
]

