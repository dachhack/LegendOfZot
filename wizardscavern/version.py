"""
Auto-generated version info. BUILD_NUMBER is incremented by pre-commit hook.
CHANGELOG is populated from recent git log entries.
"""

VERSION = "0.1.2"
BUILD_NUMBER = 331
# NOTE: Keep this list short (~8 entries). Remove old ones as new ones land.
CHANGELOG = [
    "trapped_no_d gate eased: catches stuck-but-not-quite-90% explorers (build 331). User screenshot of Eomer II (s=99 human, 1261T on F1, 52 kills, starved) prompted the 'high turn + low floor is suspicious' insight. Grid audit: 26 runs matched (turn >= 800 + max floor <= 2), most had loop_turns = 0 because they weren't 2-tile oscillations -- they were diffuse wandering across a small reachable region. s=88 dwarf F2: 28/32 reachable tiles visited (reach_pct = 85%), no new tile in 2000+ turns, BUT trapped_no_d required reach_pct >= 90 so the warp escape valve never fired -- agent burned 2500+ turns wandering until starvation. Eased the gate to two firing paths: (1) reach_pct >= 80% AND no new tile in 200T (was 90% / 200T), (2) no new tile in 500T regardless of reach_pct (covers rare case where reach_pct stays low because fog reveals add BFS-reachable tiles faster than the agent walks them). Mirrored the same in warp_mode. 5000-turn grid: avg max floor 4.15 -> 4.63 (+12%), F1 deaths 23 -> 16, F8+ runs 12 -> 22 (+83%), F9 reached 6 -> 11 (+83%), suspicious-runs 26 -> 14 (-46%), loop_turns_total 16775 -> 12230 (-27%). Agents now warp out of region-split wedges instead of wandering forever, hitting deeper hazards as intended.",
    "Final log + last actions now render on every run, not just deaths (build 330). User: 'Let's always capture the final log for all runs, not just the combat deaths.' Earlier the death_block + stuck_block rendered last-30-log + last-actions for not-alive and stuck runs only -- alive-at-turn-budget runs got no log tail. Added an else-branch that emits the same log + actions tables behind a green 'Survived to T{turn}. Run ended at the turn budget, not by death.' banner. New CSS class .alive-banner (green left-border, dim background) parallels the existing .death-banner (red) and .stuck-banner (amber) so the three end-states are visually distinct. 5000-turn grid sanity: 1/159 alive run (s=1500 human, F6 final) now shows its T4996 library-grimoire-search tail, matching the layout of the 158 death runs.",
    "Stair-wedge bounce eliminated: persistent-oscillation 'u' valve + wedge-floor descent block (build 329). User: 'Seems like there is still some starvation due to looping.' Audit on the 5000-turn carnage grid: 29/71 starvation deaths had mid-run loops averaging 612 turns wasted (vs 198 for combat deaths). Worst case s=88 human spent 1701 of 1862 turns in loops -- F2 was a region-split fragment with only U at (1,1) and a single '.' neighbour at (1,2), all other tiles walls. Agent bounced between them for the entire run while the food clock drained. Three coordinated fixes: (1) Persistent-oscillation streak counter on PlaytestSession (osc_streak) survives inventory pauses if the 1-tile pause is on the existing pair, so the streak doesn't reset every time the agent eats. (2) stairs_up_mode 'u' escape valve fires when osc_streak >= 50 -- ascend back to the prior floor when wedged on the current one. Build-318's symmetric valve was reverted because of descend/ascend cancellation; the 50-turn gate fixes that (descend-side fires immediately on stair-island, ascend-side only after a confirmed wedge). (3) PlaytestSession.wedge_floors set tracks every z escaped from via the 'u' valve. stairs_down_mode + game_loop wayfinder now both strip D from priority when current_z + 1 is a known wedge -- agent stays on the safe floor exploring for warps / Scroll of Descent instead of re-descending into the same trap. s=88 human loop_turns 1701 -> 241 (-86%). 5000-turn grid: alive 1 (held), starvation 71 -> 72, combat 67 -> 66 (within RNG noise); avg max floor 4.19 -> 4.15. The deeper fix is qualitative: the death curve no longer hides 1500-turn wedges inside what looked like starvation deaths.",
    "Food pressure dialed back: hunger decay 1.0/move -> 0.7/move, meat drop 55% -> 70%, vendor Iron Rations 1 -> 3, starter Iron Rations 2 -> 4 (build 328). User chose levers (1) + (2) from the carnage-round analysis. Decay uses an integer tracker (HUNGER_DECAY_PER_MOVE=7, HUNGER_DECAY_INTERVAL=10) on character.hunger_decay_tracker so the 0.7 ratio stays deterministic without floats. 5000-turn grid before/after: avg max floor 3.54 -> 4.19 (+18% deeper), starvation deaths 96 -> 71 (-26%), combat deaths 36 -> 67 (+86%), F5-F7 deaths 21 -> 50 (+138%), F8+ runs 12 -> 15. Survival went 0% -> 0.6% (one F9 alive at 5000T). The death curve has flipped: F4 is still mostly starvation (17/26) but F6 is 14/16 combat, F7 is 12/14 combat, F8 5/6 combat -- the agents now reach the tomb-and-elite grinder where the design wants them to die. 'long runs with low survival rates' goal hit.",
    "Eat threshold 80 -> 50: less Ration waste in inventory mode (build 327). User pushed playtest to 5000 turns ('we want a lot of death and carnage'). The 5000-turn grid showed 159/159 dead, 102 starvation (64%), avg max floor 3.43. Audit of starvation deaths: agents restored 4-8 hunger/turn over their run (well above the 1/turn decay) yet spent 50-200 turns at hunger 0 -- they were binge-eating Rations at hunger 79, gaining only 21 of the 50-nut value because the hunger cap is 100. Inventory-mode eat trigger was `hunger < 80` while the game_loop open-inventory trigger was `hunger < 50`; aligned the two so each Ration delivers close to its full 50-nut payload. Modest impact: avg max floor 3.43 -> 3.54, F4 deaths 20 -> 28, F11 + F14 floors reached for the first time. Still 0% survival at 5000T. Deeper analysis shared with user: per-floor demand is ~140 nut, meat supply averages ~75 nut (55% drop rate × ~20 nut per piece × ~15 kills), so vendor floors stay surplus but vendor-less floors run a ~65 nut deficit -- two-three missed vendors compounds into starvation. Next levers under consideration: slow hunger decay, bump meat drop rate, bigger ration stacks.",
    "Player sprite picks race-filtered pool (build 326). User: 'The player sprites don't match the races outlined by the in game sprite picker.' app.py:5852 filters the character-creation picker by get_race_pool(race), but the seed-based auto-pick path in sprite_data.py:generate_player_sprite_html (and the playtest report's own copy of that logic) drew from the FULL _CHARACTERS_POOL -- so a dwarf hero could land on an elf-pool portrait, etc. Fixed both paths to filter by race first, then fall back to the full pool when a race has no curated picks. 159-seed grid sanity check: 159/159 reports now choose sprites from their race's curated subset. The (race, gender, name) seed continues to drive the deterministic pick within the filtered pool so the visual link between a report and a live launch of the same seed is preserved.",
    "Upgrade scroll: ease the failure curve (build 325). User: 'Pretty high failure rate on upgrade scrolls. Let's adjust that.' items.py:2171 had a base 70% success rate with a -15pp natural-limit penalty that bit on the very first upgrade for starter Battleaxe/Dagger (level=0, so upgrade_level >= level+1 triggers at the +1 attempt). Bumped each tier by +10pp (base 70 -> 80, +3 tier 60 -> 70, +6 65, +10 55, +15 45) and softened the natural-limit penalty 15pp -> 8pp. Net effect at Lv1 / INT 10: first upgrade ~90% (was ~79%), subsequent +1->+2 ~73% (was ~66%). 159-seed grid: weapon end-state upgrades 103 -> 116 (+13%), armor 11 -> 14, +2 weapons 22 -> 27, +3 weapons 5 -> 6. Lower failure means more equipped-slot retention; the still-modest end-state totals reflect that scrolls land before mid-run weapon swaps and the upgrades stay with the dropped item.",
    "Food table: Cooked column + weapon/armor upgrade rebalance (build 324). User: 'do found, bought, cooked, eaten, hunger restored for the food and cooking', plus flagged 'Erkenbrand the Tall used 6 scrolls up upgrade but only had one +2 weapon and no +n armor.' (1) Added a Cooked column to the per-run Food &amp; Cooking table between Bought and Eaten. Cook events from the items.py:2990 'You cooked the X!' log now go through _normalize_meat_name -- the same normaliser eat events use -- so e.g. 'Cooked Gnoll Burger' (cook log) and 'gamey Gnoll burger' (eat log) collapse into the same 'Cooked Gnoll burger' row. Normaliser uses the known cut suffix list (burger / chops / cold cuts / filet / kebab / nuggets / roast / skewer / steak) to find the cut, then takes the trailing capitalised word(s) as the monster name. (2) Erkenbrand audit found: 6 scrolls used, 2 successful weapon upgrades on Longsword (T400, T637), 0 armor upgrades, then T1341 swapped to a cursed Worldbreaker that orphaned the +2 Longsword. Root cause #1: smart_policy targeted the equipped WEAPON only, never armor. Round-robin would over-correct (50/50 split lost too much weapon power), so the new rule is weapon-first with armor catch-up when armor lags weapon by 2+ upgrades. Root cause #2: obs.inventory is filter-narrowed in upgrade_scroll_mode (the 'use' filter hides Weapon/Armor), so the policy couldn't find equipped slots. Added equipped_weapon_gear_idx / equipped_armor_gear_idx to obs.player.equipped, computed off the unfiltered sorted view -- the same ordering process_upgrade_scroll_action builds its menu from. 159-seed grid: 0 armor upgrades -> 11 armor upgrades (avg 0.07/run), weapon upgrades -103 (still solid), depth stable at avg 3.42.",
]
