"""
Auto-generated version info. BUILD_NUMBER is incremented by pre-commit hook.
CHANGELOG is populated from recent git log entries.
"""

VERSION = "0.1.2"
BUILD_NUMBER = 224
# NOTE: Keep this list short (~8 entries). Remove old ones as new ones land.
CHANGELOG = [
    "Race-flavored magic progression in wizardscavern/characters.py:max_mana. Magic is now a race-archetype dimension: Elf is the natural caster, Human is a slow scaler, Dwarf almost never casts. Default base INT keeps every fresh character (human 10, elf 12, dwarf 8) below the gate so race comes through only when the player invests in INT (oracles, potions, level-ups). Elf gate int>15 at +4/int + 10 floor (int=16->14 MP, int=18->22 MP, int=25->50 MP). Human gate int>15 at +3/int + 5 floor (int=16->8 MP, int=18->14 MP, int=25->35 MP). Dwarf gate raised to int>20 at +2/int + 3 floor (int=21->5 MP, int=25->13 MP). Endgame ceilings respectively 50/35/13 MP. Also fixed a playtest_harness bug surfaced by the same QA pass: random_policy.spell_casting_mode was hardcoded to slot 1, so Fireball/Heal never got tested -- now picks a random affordable slot from obs.memorized_spells",
    "Spellcaster balance pass (driven by headless playtest run): the early-game caster was mana-starved -- across 5 random-policy Elf runs the player cast exactly 1 spell per run before running dry, and Fireball/Heal were never reachable on floor 1. Three coordinated fixes: (1) L0 cantrips dropped by ~3 mana each in wizardscavern/items.py:3433-3438 (Ice Shard 8->5, Spark 5->3, Stone Throw 6->4, Minor Heal 8->5, Shadow Bolt 7->4, Light Ray 7->4) so a fresh caster can afford 2-3 cantrips per fight. (2) max_mana floor bumped in wizardscavern/characters.py:874 -- new curve `(int-15)*4 + 10` keeps the int>15 caster gate but starts at 14 MP at int=16 (was 5) and lands at 22 MP at int=18 (was 15), endgame ceiling of 50 MP at int=25 is preserved. (3) Out-of-combat mana regen: new `process_mana_regen` in items.py ticks 1 MP per 5 moves while not starving, hooked into move_player alongside process_hunger. Re-run playtest shows 4 casts in first 32 turns vs 1 in 200 previously",
    "Playtest harness: race + spellcaster support. new_game() / CLI now accept --race {human,elf,dwarf} (applies the same stat mods as the UI's character-creation flow -- elf -10 HP +2 INT/DEX, dwarf +20 HP +2 STR/ATK/DEF -2 INT/DEX), --int-bonus N (added to INT after race mods so an Elf can clear the int>15 spell-casting threshold), and --spells \"Ice Shard,Heal,...\" (pre-memorize by SPELL_TEMPLATES name). Wired spell_casting_mode into step() dispatch so the random policy can cast 'c' -> slot number in combat. Observation gained `memorized_spells` list and `player.race/intelligence` fields",
    "Headless playtest harness: new wizardscavern/playtest_harness.py drives the game logic without the Toga UI -- observe()/step(action) over gs.* globals, mode-sensitive action dispatch (game_loop/combat_mode/chest_mode/etc.), CLI with --seed, --turns, --policy {random,script}, --playtest-mode, --jsonl. Smoke-test: `python3 -m wizardscavern.playtest_harness --seed 42 --turns 200`. Paired with a `.claude/agents/playtester.md` subagent definition so the Claude Code playtester can be spawned to hunt crashes / softlocks / balance issues without touching the UI",
    "Cap Zotle Dimensional Key + random warp at floor 50: the teleporter had no upper-bound on target_z so 'x,y,98' would call add_floor() in a loop and generate phantom floors past the boss arena. Those phantom floors are outside the balance curve (Mythic evolution catches every spawn at 2.5x multipliers) so they crush even god-mode characters. Added MAX_FLOOR_Z = 49 cap in process_zotle_teleporter_action with a flavor message ('the key fizzles -- Zot's experiments only mapped 50 floors') and clamped max_z in _execute_warp so the random warp portal also stops at floor 50. Updated key description + prompt to mention 'floors 1-50'",
    "Warp screen + playtest power-up: warp_mode was rendering without the bottom-pinned-zone wrapper so it scrolled vertically. Wrapped it. Resist/Enter flipped back to vertical .altar-actions (stacked, not side-by-side) for the narrative beat. Playtest mode (Tourist) bumped to level 50 / HP 9999 / MP 9999 / ATK 250 / DEF 250 / STR DEX INT 99 / gold 999999 for comfortable endgame testing",
    "Intro screens lock to viewport: main_menu / intro_story and the splash screen both used min-height: 100vh which let content push the page taller and scroll. Switched both to height: 100vh + overflow: hidden + box-sizing: border-box. Save-slots panel inside the intro is flex-shrink: 0 with max-height: 50vh / overflow-y: auto so many saves scroll within that panel rather than the whole screen",
    "Splash + character creation polish: splash 'LATEST' changelog box now sits right under the version line at the top, Enter the Cavern chip pinned to bottom via margin-top: auto. Player name length cap 12 -> 13 characters. Character-creation keyboard uses uniform 32px keys with natural 10/9/7-row taper",
    "Altar / Oracle info split: stripped the piety bar (★★☆ Tier N) and the Pray-reward subtitle preview off the altar UI -- altar now just shows god name, what it craves, and Pray/Sacrifice chips. Moved the per-god piety + tier-reward readout to the Oracle: tap the new PANTHEON chip alongside Gaze and the mirror logs all 8 gods with their piety stars, tier progress, and the current Pray reward. Also tightened the altar init log to skip the inline tier breakdown",
    "Altar rework: per-god identification + piety tier system. Each altar identified up-front (no more guess-the-god). Two chips: PRAY (claim current tier reward, altar consumed) and SACRIFICE (matching items grant +1 piety, wrong items returned uncostly). Tiers unlock at 1/3/5 sacrifices. 4 distinct Pray rewards per god across T0-T3 (see design/altar_rewards.csv). Rune of Devotion auto-drops when all 8 gods reach T3",
    "Warp portal panel compression: same overflow as grimoire/shrine/tomb -- 'Enter the Portal' button was clipped. Applied the .altar-actions.dual treatment, tightened padding, shortened flavor text",
    "Compress 3 panels + DROP ROTTEN chip. library_read_decision_mode, shrine_mode, and tomb_mode were overflowing the 200px room-panel slot. Added .altar-actions.dual CSS for binary-choice rooms, tightened padding, shortened labels. Also wired 'dr' command + DROP ROTTEN chip in inventory's Eat filter to bulk-discard rotten Meat",
    "Stats banner HTML bars: replaced the ASCII '[######----] 999/999' HP / MP bars with compact coloured HTML bars (.statbar). Fill shifts green -> orange -> red as HP drops, MP bar is blue. ~70px each (vs ~150px for the text version at width=10), so the stats line no longer wraps at high values",
    "Chip overflow target swap: previous round mistakenly tightened combat-bar -- combat was already fine. The actual overflow is on the inventory bar (5 chips: CRAFT / SPELLS / JOURNAL / QUIT / CLOSE) when spells are available. Reverted INVENTORY label + combat-bar class in combat back to default sizing; moved the tighter chip styling (padding 8x10, font 12, min-height 38, radius 14) onto .hudchips.inv-bar instead so the inventory chips fit on one line",
    "Three quick fixes: pinned the game log to a fixed 130px so it stays the same size across map / inventory / vendor / picker views; #content-area is now flex: 1 1 auto. Playtest mode (Tourist) now sets base_max_health_bonus=839 + level=10 + str=30 to land on exactly 999/999 HP and 999/999 MP (was setting health=9999 with only ~160 max HP)",
    "Layout tightening: with the post-font-bump altar / library / dungeon panels still using only ~150-160px of the 240px .room-panel slot, dropped the uniform interaction box 240 -> 200px. Also stripped bottom-pinned-zone padding 2/4 + border-top, and content-area padding 4 -> 0, so the map sits flush against the room-panel and the chips flush against the log",
    "Room interaction font bump: with the .room-panel slot at a uniform 240px the previous 9-12px text was small and sparse-feeling. Bumped fonts across all room interactions -- empty floor flavor 12 -> 14px, lantern line 11 -> 13px, altar god name 12 -> 16px, altar title/target 9 -> 12px, .taprow.altar-act action card name 12 -> 14px, meta line 10 -> 12px, altar-actions.compact chip name 10 -> 13px / meta 8 -> 10px. Also widened inner padding 6 -> 8/10px so larger text isn't cramped against borders",
    "Channeling indicator placement: the CHANNELING: <spell> [..] N turns box used to render as a third row inside the combat room-panel below the player card, which pushed the map down and ate vertical space. Moved it inside the player_panel as an absolutely-positioned overlay anchored to the right (where attack/defense dice render during combat rounds) -- since no dice are rolling while the player is concentrating, that real-estate is free",
    "Layout overhaul: switched from fixed-positioned top-strip + log + bottom-pinned-zone (with a dead-space gap floating between the stats banner and the interaction box) to a body-level flex column. Body is now display: flex / height: 100vh / overflow: hidden; top-strip is flex: 0 0 auto (natural height), content-area is flex: 0 0 auto (hugs its content), and game-log is flex: 1 1 auto (fills whatever vertical space is left). Combat boxes and room interaction boxes now share a single uniform 240px .room-panel slot pinned right below the stats bar, with the map + chips below, and the log expanded back into the previously-dead bottom area",
]
